 1. 답1 : 자바는 다중상속을 허용하지 않는다. 부모 클래스는 하나.

 2. 답2 : 부모 객체는 항상 자식 타입으로 강제 변환 될 수 없다. 자식 객체가 부모타입으로 변경되었을 경우에만 가능.

 3. 답1 : final 클래스는 부모 클래스로 사용할 수 없다.

 4. 답2 : 부모 메서드보다 접근제한이 같거나 풀어져야 한다. more public

 5. 답2 : 리턴타입은 무관하다.

 6. 답2 : 인스턴스 필드는 생성자에서 초기화 될수 있지만 정적블록(static block)에서는 초기화 될 수 없다. 

 7. 답3 : default 접근 제한은 해당 클래스 내부 뿐만 아니라 해당 패키지까지 사용 허가. 지문에 있는 내용은 private

 8. 답 : day12.quiz 폴더 Parent, Child 참조. 

 9. 답 : day09.inherit.quiz 폴더 참조. 

10. 답 : day12.quiz 폴더 main 참조.
